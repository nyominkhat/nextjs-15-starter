import PageWrapper from '@/components/PageWrapper';
import BlogList from '@/app/[locale]/(views)/blog/blog-list';

const Blog = () => {
  return (
    <PageWrapper>
      <BlogList />
    </PageWrapper>
  );
};

export default Blog;
